[build]
  command = "cd artifacts/vemat && npm install --legacy-peer-deps && npm run build"
  publish = "artifacts/vemat/dist/public"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
